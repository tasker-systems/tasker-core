# TaskTemplate Configuration - DSL Variant - TypeScript Implementation
#
# Retryable Error Handler for E2E Testing
# Demonstrates retry behavior via TypeScript FFI
#
# Template: test_scenarios/retryable_error_only_ts:1.0.0
# Implementation: TypeScript FFI with tasker-ts (Bun FFI)
#
---
name: retryable_error_only_dsl_ts
namespace_name: test_scenarios_dsl_ts
version: 1.0.0
description: "Single-step handler that returns retryable errors (TypeScript)"
metadata:
  author: TypeScript FFI Implementation
  tags:
    - namespace:test_scenarios_dsl_ts
    - pattern:single_step
    - implementation:typescript_ffi
    - language:typescript
    - test_type:retryable_error
    - tas:TAS-105
  created_at: "2025-12-23T00:00:00Z"
  updated_at: "2025-12-23T00:00:00Z"
system_dependencies:
  primary: default
  secondary: []
domain_events: []
input_schema:
  type: object
  properties:
    error_message:
      type: string
      description: "Error message to return"
steps:
  - name: retryable_error_step_dsl_ts
    description: "A step that returns a retryable error"
    handler:
      callable: test_errors_dsl.step_handlers.RetryableErrorDslHandler
      initialization:
        expected_outcome: retryable_error
    system_dependency:
    dependencies: []
    retry:
      retryable: true
      max_attempts: 3
      backoff: exponential
      backoff_base_ms: 100
      max_backoff_ms: 1000
    timeout_seconds: 30
    publishes_events: []
environments:
  test:
    steps:
      - name: retryable_error_step_dsl_ts
        timeout_seconds: 10
        retry:
          max_attempts: 2
